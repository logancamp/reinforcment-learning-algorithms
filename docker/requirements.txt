# Mostly pinned from the working ppo_env venv (macOS/arm64) found in the
# project. torch is installed separately in the Dockerfile (CPU vs CUDA wheel
# differ). numpy is left unpinned on purpose: opencv-python's Linux/aarch64
# wheel declares numpy>=2, which conflicts with the numpy==1.26.4 that was
# frozen from the macOS build (whose opencv wheel apparently had different
# metadata). Let pip's resolver pick a numpy that satisfies everything below,
# same as the original (unpinned) Makefile install did.
numpy
gymnasium==1.3.0
gymnasium[classic-control]
gymnasium[atari]
gymnasium[mujoco]
ale-py==0.11.2
autorom==0.6.1
mujoco==3.8.0
tensorboard==2.20.0
optuna==4.8.0
opencv-python==4.13.0.92
imageio==2.37.3
pygame==2.6.1